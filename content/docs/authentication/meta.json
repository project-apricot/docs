{
  "title": "Authentication",
  "description": "Both directions of a microservice's authentication from one settings section: the tokens it accepts, and the tokens it presents to the services it calls — as itself, or as the party it is serving.",
  "pages": ["index", "usage", "aspnetcore", "token-exchange", "error-definitions"]
}
